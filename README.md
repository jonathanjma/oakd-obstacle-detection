### OAK-D Obstacle Detection

Converts depth data from an OAK-D stereo camera and sends it to Ardupilot over Mavlink. Can be run as a Docker container on the BlueBoat BlueOS platform. Also supports sending camera frames over RTSP for debugging.